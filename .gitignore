# 운영체제 관련 파일
.DS_Store # macOS
Thumbs.db # Windows

# 빌드 및 컴파일 결과 디렉토리
# (프로젝트마다 다를 수 있으나, 일반적으로 소스 코드가 아닌 생성된 파일들은 제외)
/bin/
/obj/
/dist/
/out/
/build/

# 패키지 관리자 관련 파일/폴더
# (예: Node.js, Python, Java 등)
/node_modules/ # Node.js
*.egg-info/ # Python
.venv/ # Python virtual environment
/vendor/ # PHP, Go (경우에 따라)

# 로그 및 임시 파일
*.log
*.tmp
temp/
tmp/

# IDE/에디터 관련 설정 파일
# (개인 설정 파일이므로 프로젝트 공유 X)
.vscode/ # VS Code
.idea/ # IntelliJ, WebStorm 등 JetBrains IDE
*.swp # Vim
*.swo # Vim

# 민감한 정보 및 개인 환경 설정 파일 (가장 중요)
# (비밀번호, API 키, 데이터베이스 정보 등 포함 가능)
# 이 파일들은 절대 공개 저장소에 푸시되면 안 됩니다.
.env # 환경 변수 파일
config.local.js
local.properties
settings.local.py

# 리소스 파일
resource/
